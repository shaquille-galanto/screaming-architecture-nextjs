import clsx from 'clsx'
{{#isEqual isReusable "yes"}}
import { forwardRef } from 'react'
{{/isEqual}}
import styles from './styles.module.scss'
import type { {{pascalCase name}}Props } from './types'

export const {{pascalCase name}} = 
{{#isEqual isReusable "yes"}}
  forwardRef<HTMLDivElement, {{pascalCase name}}Props>(({ className, ...props }, ref) => (
    <{{ element }} className={clsx(styles.wrapper, className)} ref={ref} {...props}>
      {{pascalCase name}}
    </{{ element }}>
  ))
{{else}}
  ({ className, ...props }: {{pascalCase name}}Props) => (
    <{{ element }} className={clsx(styles.wrapper, className)} {...props}>
      {{pascalCase name}}
    </{{ element }}>
  )
{{/isEqual}}